"use strict"; //treated all js code as newer version.

//alert("hello") // different syntax in node, can be used in browser

let name="ankit";

let age=20;

let isLoggedIn=true;

/*
primitive datatypes:-
string :- can be in both " " and ' '
number:- at max upto 2^53
boolean :- true/false
big int :- for big size
null :-value assigned as null, stand alone value, representation of empty value.
undefined :-value not assigned
symbol :- for uniqueness
*/

//object

console.log(typeof "ankit"); // string
console.log(typeof null); // object
console.log(typeof undefined);   // undefined

//typeof as name suggests find the type.