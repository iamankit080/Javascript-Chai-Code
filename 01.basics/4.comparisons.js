console.log(null==0);
console.log(null>=0);
console.log(null>0);

// compariosn operator convert null into 0.
//note these conversion are avoided, because of confusion.

// strict check(===)--check value and datatype.
